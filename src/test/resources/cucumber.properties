#this properties file controls the generation of a cloud based report

#cucumber.publish.quiet=true
#to disable the default report message displayed on each run in the console

cucumber.publish.enabled=true
#to enable publishing a temporary report on the cloud with a unique link to be conveniently shared with others, currently disabled since the report is being generated locally in target folder